worker_processes  auto;
events { worker_connections  1024; }

http {
  include       /etc/nginx/mime.types;
  default_type  application/octet-stream;
  sendfile      on;
  tcp_nopush    on;
  tcp_nodelay   on;
  keepalive_timeout  65;
  server_tokens off;

  # Gzip（視需求調整）
  gzip on;
  gzip_comp_level 5;
  gzip_min_length 1024;
  gzip_types text/plain text/css application/javascript application/json application/xml+rss image/svg+xml;

  server {
    listen 80 default_server;
    server_name _;

    # 靜態檔案
    root /usr/share/nginx/html;

    # 若前端路由是 HTML5 history，需要 fallback
    location / {
      try_files $uri $uri/ /index.html;
    }

    # ⚠️ 生產環境建議由 Ingress 做 /api 反代
    # 若要在容器內本地測試代理，取消下段註解並改成你的後端位址
    # location /api/ {
    #   proxy_http_version 1.1;
    #   proxy_set_header Upgrade $http_upgrade;
    #   proxy_set_header Connection "upgrade";
    #   proxy_set_header Host $host;
    #   proxy_pass http://backend-dev:8080; # for docker-compose dev only
    # }
  }
}
